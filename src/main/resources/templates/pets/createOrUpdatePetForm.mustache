{{!
<!DOCTYPE html>

<html lang="en">

  <body>}}

{{#layout}}{{#menu}}Owners{{/menu}}

{{#pet}}
  <h2>
    {{#new}}New {{/new}}Pet
  </h2>
  <form class="form-horizontal" method="post">
    <input type="hidden" name="id" th:value="*{id}" />
    <div class="form-group has-feedback">
      <div class="form-group">
        <label class="col-sm-2 control-label">Owner</label>
        <div class="col-sm-10">
          {{#owner}}<span>{{firstName}} {{lastName}}</span>{{/owner}}
        </div>
      </div>
      {{#form.pet}}
        {{#inputField}}Name <input name="name"/>{{/inputField}}
        {{#inputField}}Birth Date <input name="birthDate" type="date"/>{{/inputField}}
        {{#selectField}}Type <input name="type" values="types"/>{{/selectField}}
      {{/form.pet}}
    </div>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <button class="btn btn-primary" type="submit">{{#new}}Add{{/new}}{{^new}}Update{{/new}}
          Pet</button>
      </div>
    </div>
  </form>
{{/pet}}

{{/layout}}

{{!  </body>

</html>}}
